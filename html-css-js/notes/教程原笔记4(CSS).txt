1. float浮动？

    脱离文档流，沿着父容器靠左或靠右进行排列，如果之前已经有浮动的元素，会挨着浮动的元素进行排列。当父容器内的所有容器都浮动了，那么父容器可能会在大小上有较大变化（如面积变成直线等）

        取值：left、right、none

    float注意点：
        只会影响后面的元素
        内容默认提升半层（可以做图文混排效果）
        不给宽度设定时，默认宽根据内容决定
        换行排列
        主要给块元素，但是也可以给内联元素添加
